<%= simple_form_for ([pool, booking]) do |f| %>
  <%= f.input :starting_date, as: :string, required: false, input_html: {class: "datepicker", id: "starting-date"} %>
  <%= f.input :ending_date, as: :string, required: false, input_html: {class: "datepicker", id: "ending-date"} %>
  <%= f.submit "Send request", class: "btn btn-primary" %>
<% end %>

<!-- useless comment -->
